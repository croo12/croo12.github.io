# Remove stale .gitkeep in widgets directory

**Category:** Architecture / Cleanup
**Priority:** Low
**File:** `src/widgets/.gitkeep`

## Problem
`.gitkeep` exists alongside real code (`src/widgets/layout/`). It is no longer needed.

## Suggested Fix
Delete `src/widgets/.gitkeep`.
