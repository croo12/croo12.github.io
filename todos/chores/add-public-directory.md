# Create public/ directory with static assets

**Category:** QoL
**Priority:** Medium
**File:** (new) `public/`

## Problem
No `public/` directory. No place for `robots.txt`, `favicon.ico`, `manifest.webmanifest`.

## Suggested Fix
Create `public/` with minimum `robots.txt` and `favicon.ico`.
