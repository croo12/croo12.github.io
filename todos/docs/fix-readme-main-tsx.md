# Fix README entry point reference

**Category:** Documentation
**Priority:** Low
**File:** `README.md`

## Problem
README refers to entry point as `main.ts` instead of `main.tsx`.

## Suggested Fix
Change `main.ts` to `main.tsx`.
