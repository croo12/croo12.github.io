# Fix README to use yarn instead of npm

**Category:** Documentation
**Priority:** Medium
**File:** `README.md`

## Problem
README documents `npm install` and `npm run` but project uses Yarn exclusively.

## Suggested Fix
Replace all `npm` references with `yarn` commands.
