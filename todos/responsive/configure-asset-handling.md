# Configure Vite asset handling for future game assets

**Category:** Responsive Design / Build
**Priority:** Low
**File:** `vite.config.ts`

## Problem
No `build.assetsInlineLimit` or responsive image handling configured for future game assets (sprites, images).

## Suggested Fix
Configure `assetsInlineLimit` and consider image optimization plugins when assets are added.
