# Add favicon and apple-touch-icon

**Category:** SEO / Assets
**Priority:** Medium
**File:** `index.html`, `public/` (missing)

## Problem
No favicon at all. No `public/` directory exists.

## Suggested Fix
Create `public/` directory with `favicon.ico` and `apple-touch-icon.png`. Add link tags to `index.html`.
